# v0.5 sidecar 不需要新增任何秘密。
#
# Telegram token 與使用者 allowlist 應透過 `hermes gateway setup` 設定，並由
# Hermes 原生 Telegram adapter 管理。不要在 plugin settings、Git、SQLite 或
# runner command line 中複製 token。
#
# 若你從 v0.4 升級，舊的以下變數不再由推薦的 sidecar 模式使用：
# TELEGRAM_CANONICAL_BRIDGE_BOT_TOKEN
# TELEGRAM_CANONICAL_BRIDGE_BACKEND_TOKEN
#
# 確認原生 outbound route 的方式：
# hermes send --list telegram
