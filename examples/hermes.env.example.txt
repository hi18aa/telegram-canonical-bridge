# v0.6 不需要新的 Telegram 或 Desktop 秘密。
#
# Telegram token 與 allowlist 由 Hermes 原生 `gateway setup` 管理；不要把 token
# 複製到 plugin settings、Git、SQLite 或 runner command line。
#
# 選填：只有需要把 ledger 放到自訂位置時才設定。Controller 與所有本機 worker
# 必須看見相同的絕對路徑。
# HERMES_AGENT_TASK_STATE_PATH=C:\path\to\tasks.sqlite3
